services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=feedverse'
#      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=1234'
      - 'MYSQL_USER=root'
    ports:
      - '3306'
